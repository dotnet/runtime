<Project>
  
  <!-- TODO: (Consolidation) Remove when consolidated. -->
  <Import Project="$(MSBuildThisFileDirectory)..\Directory.Build.targets" Condition="'$(IsRuntimeRepository)' == 'true'" />
  <Import Project="$(MSBuildThisFileDirectory)..\src\Directory.Build.targets" Condition="'$(IsRuntimeRepository)' != 'true'" />

</Project>