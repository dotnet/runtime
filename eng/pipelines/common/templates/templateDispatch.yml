parameters:
  - name: templatePath
    type: string
  - name: stages
    type: stageList
  - name: containers
    type: object

extends:
  template: ${{ parameters.templatePath }}
  parameters:
    stages: ${{ parameters.stages }}
    containers: ${{ parameters.containers }}