parameters:
  condition: false
  extraBuildArgs: ''
  isExtraPlatformsBuild: false
  nameSuffix: ''
  platforms: []
  publishArtifactsForWorkload: false

jobs:

#
# Build for Browser/wasm
#
- template: /eng/pipelines/common/platform-matrix.yml
  parameters:
    jobTemplate: /eng/pipelines/common/global-build-job.yml
    buildConfig: Release
    runtimeFlavor: mono
    platforms: ${{ parameters.platforms }}
    variables:
      # map dependencies variables to local variables
      - name: workloadSubsetArg
        ${{ if eq(parameters.publishArtifactsForWorkload, true) }}:
          value: '+mono.wasmworkload'
        ${{ else }}:
          value: ''

    jobParameters:
      isExtraPlatforms: ${{ parameters.isExtraPlatformsBuild }}
      testGroup: innerloop
      nameSuffix: ${{ parameters.nameSuffix }}_BuildOnly
      buildArgs: -s mono+libs+packs$(workloadSubsetArg) -c $(_BuildConfig) /p:BrowserHost=$(_hostedOs) ${{ parameters.extraBuildArgs }} /p:TestWasmBuildTests=true /p:TestAssemblies=false /p:ArchiveTests=true
      timeoutInMinutes: 120
      condition: ${{ parameters.condition }}
      extraStepsTemplate: /eng/pipelines/common/wasm-post-build-steps.yml
      extraStepsParameters:
        publishArtifactsForWorkload: ${{ parameters.publishArtifactsForWorkload }}
