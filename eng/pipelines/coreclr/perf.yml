parameters:
- name: perfBranch
  displayName: Performance Repo Branch
  type: string
  default: main
- name: crossgenEnabled
  displayName: Crossgen Enabled
  type: boolean
  default: true
- name: crossgen
  displayName: Crossgen Configs
  type: object
  default:
    configs:
      - windows_x64
      - windows_x86
- name: viperJitOptRepeatEnabled
  displayName: Viper Jit Opt Repeat Enabled
  type: boolean
  default: true
- name: viperJitOptRepeat
  displayName: Viper Jit Opt Repeat Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
- name: viperMicroEnabled
  displayName: Viper Micro Enabled
  type: boolean
  default: true
- name: viperMicro
  displayName: Viper Micro Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
- name: owlMicroEnabled
  displayName: Owl Micro Enabled
  type: boolean
  default: true
- name: owlMicro
  displayName: Owl Micro Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
- name: tigerNoR2REnabled
  displayName: Tiger No R2R Enabled
  type: boolean
  default: true
- name: tigerNoR2R
  displayName: Tiger No R2R Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
- name: tigerNoPGOEnabled
  displayName: Tiger No PGO Enabled
  type: boolean
  default: true
- name: tigerNoPGO
  displayName: Tiger No PGO Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
- name: tigerMicroEnabled
  displayName: Tiger Micro Enabled
  type: boolean
  default: true
- name: tigerMicro
  displayName: Tiger Micro Configs
  type: object
  default:
    configs:
      - windows_x64
      - linux_x64
      - windown_x86
      - linux_musl_x64
- name: tigerMonoAOTEnabled
  displayName: Tiger Mono AOT Enabled
  type: boolean
  default: true
- name: tigerMonoAOT
  displayName: Tiger Mono AOT Configs
  type: object
  default:
    configs:
      - linux_x64
- name: tigerMonoInterpreterEnabled
  displayName: Tiger Mono Interpreter Enabled
  type: boolean
  default: true
- name: tigerMonoInterpreter
  displayName: Tiger Mono Interpreter Configs
  type: object
  default:
    configs:
      - linux_x64
- name: tigerMonoEnabled
  displayName: Tiger Mono Enabled
  type: boolean
  default: true
- name: tigerMono
  displayName: Tiger Mono Configs
  type: object
  default:
    configs:
      - linux_x64
- name: androidMonoEnabled
  displayName: Android Mono Enabled
  type: boolean
  default: true
- name: androidMono
  displayName: Android Mono Configs
  type: object
  default:
    configs:
      - android_arm64

trigger:
  batch: true
  branches:
    include:
    - main
    - release/9.0
    - release/8.0
  paths:
    include:
    - '*'
    - src/libraries/System.Private.CoreLib/*
    exclude:
    - '**.md'
    - .devcontainer/*
    - .github/*
    - docs/*
    - LICENSE.TXT
    - PATENTS.TXT
    - THIRD-PARTY-NOTICES.TXT

variables:
- template: /eng/pipelines/common/variables.yml

#
# For the 'schedule' case, only wasm/jsc perf jobs are run.
# And the rest are build jobs - wasm, mono, coreclr, and libraries.
#
# Since, we are not running *any* perf jobs, none of these builds are needed,
# thus the whole scheduled run can be disabled.
#
schedules:
- cron: "0 0 * * 1"
  displayName: Weekly Monday 12am UTC Build
  branches:
    include:
    - main
  always: true

extends:
  template:  /eng/pipelines/common/templates/pipeline-with-resources.yml
  parameters:
    stages:
    - stage: Build
      jobs:

      - template: /eng/pipelines/coreclr/perf-wasm-jobs.yml
        parameters:
          collectHelixLogsScript: ${{ variables._wasmCollectHelixLogsScript }}
          perfBranch: ${{ parameters.perfBranch }}
          #${{ and(ne(variables['System.TeamProject'], 'public'), in(variables['Build.Reason'], 'Schedule')) }}:
          #  runProfile: 'non-v8'
          ${{ if ne(variables['System.TeamProject'], 'public') }}:
            runProfile: 'v8'

      - template: /eng/pipelines/coreclr/perf-non-wasm-jobs.yml
        parameters:
          perfBranch: ${{ parameters.perfBranch }}
          crossgen: ${{ parameters.crossgen }}
          crossgenEnabled: ${{ parameters.crossgenEnabled }}
          viperJitOptRepeat: ${{ parameters.viperJitOptRepeat }}
          viperMicro: ${{ parameters.viperMicro }}
          owlMicro: ${{ parameters.owlMicro }}
          tigerNoR2R: ${{ parameters.tigerNoR2R }}
          tigerNoPGO: ${{ parameters.tigerNoPGO }}
          tigerMicro: ${{ parameters.tigerMicro }}
          tigerMonoAOT: ${{ parameters.tigerMonoAOT }}
          tigerMonoInterpreter: ${{ parameters.tigerMonoInterpreter }}
          tigerMono: ${{ parameters.tigerMono }}
          androidMono: ${{ parameters.androidMono }}
