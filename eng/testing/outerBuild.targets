<Project>
  <Target Name="VSTest"
          DependsOnTargets="GetProjectWithBestTargetFrameworks">
    <MSBuild Projects="@(InnerBuildProjectsWithBestTargetFramework)"
             Targets="VSTest"
             BuildInParallel="$(BuildAllProjects)" />
  </Target>
</Project>