name: Mono experiments

on:
 workflow_dispatch:

jobs:
  build:
    runs-on: macos-10.15

    steps:

    - run: cat ~/.config/NuGet/NuGet.Config

    - run: mono --version

    - run: nuget
      
    - run: cat ~/.config/NuGet/NuGet.Config
