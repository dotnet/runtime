<Project>
  <Target Name="PrepareForWasmBuild" BeforeTargets="WasmBuildApp">
    <ItemGroup>
      <WasmAssemblies Include="$(TargetDir)publish\*.dll" />
    </ItemGroup>
  </Target>
  <Import Project="$(MonoProjectRoot)\wasm\build\WasmApp.InTree.targets" />
</Project>
