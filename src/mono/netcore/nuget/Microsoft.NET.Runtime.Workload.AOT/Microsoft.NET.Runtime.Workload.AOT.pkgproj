<Project DefaultTargets="Build">
  <Import Project="$([MSBuild]::GetPathOfFileAbove(Directory.Build.props))" />

  <PropertyGroup>
    <PackageDescription>The workload pack for the MonoAOTCompiler MSBuild Task</PackageDescription>
  </PropertyGroup>

  <ItemGroup>
    <_ManifestFile Include="WorkloadManifest.json" />

    <PackageFile Include="@(_ManifestFile)" TargetPath="\" />
  </ItemGroup>

  <Import Project="$([MSBuild]::GetPathOfFileAbove(Directory.Build.targets))" />
</Project>