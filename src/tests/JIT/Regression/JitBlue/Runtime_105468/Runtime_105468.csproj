<Project Sdk="Microsoft.NET.Sdk">
  <!-- This test must be in its own assembly as it sets SkipLocalsInit on the module. -->
  <PropertyGroup>
    <Optimize>True</Optimize>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="$(MSBuildProjectName).cs" />
  </ItemGroup>
</Project>
