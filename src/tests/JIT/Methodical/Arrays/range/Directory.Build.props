<Project>
  <PropertyGroup>
    <BuildAsStandalone>false</BuildAsStandalone>
  </PropertyGroup>

  <Import Project="$([MSBuild]::GetPathOfFileAbove(Directory.Build.props, '$(MSBuildThisFileDirectory)..'))" />

  <PropertyGroup>
    <AssemblyName>$(MSBuildProjectName.Replace("_d", "").Replace("_r", ""))</AssemblyName>
  </PropertyGroup>
</Project>