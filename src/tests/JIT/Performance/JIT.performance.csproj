<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Test runs too long in interpreter-only mode -->
    <DisableProjectBuild Condition="'$(TargetArchitecture)' == 's390x'">true</DisableProjectBuild>
    <DisableProjectBuild Condition="'$(TargetArchitecture)' == 'ppc64le'">true</DisableProjectBuild>
  </PropertyGroup>

  <ItemGroup>
    <MergedWrapperProjectReference Include="*/**/*.??proj" />
  </ItemGroup>

  <Import Project="$(TestSourceDir)MergedTestRunner.targets" />
</Project>
