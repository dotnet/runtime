<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RequiresProcessIsolation>true</RequiresProcessIsolation>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <UseAppHost>true</UseAppHost>
    <CLRTestTargetUnsupported>true</CLRTestTargetUnsupported>
    <CLRTestTargetUnsupported Condition="'$(TargetsWindows)' == 'true' OR '$(TargetsMacCatalyst)' == 'true' OR ('$(TargetsUnix)' == 'true' AND '$(TargetsMobile)' != 'true')">false</CLRTestTargetUnsupported>
    <DisableProjectBuild>true</DisableProjectBuild>
    <DisableProjectBuild Condition="'$(TargetsWindows)' == 'true' OR '$(TargetsMacCatalyst)' == 'true' OR ('$(TargetsUnix)' == 'true' AND '$(TargetsMobile)' != 'true')">false</DisableProjectBuild>
    <DefineConstants>$(DefineConstants);INTENTIONALLY_FAIL</DefineConstants>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="..\GitHub_87879\test87879.cs" />
  </ItemGroup>
</Project>
