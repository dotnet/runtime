<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Override the async directory's DisableProjectBuild setting -->
    <!-- This test uses real classes (not Reflection.Emit) so it can run on all platforms -->
    <DisableProjectBuild>false</DisableProjectBuild>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="$(MSBuildProjectName).cs" />
    <Compile Include="LargeClassWithIntMethods.cs" />
    <Compile Include="LargeClassWithTaskMethods_Success.cs" />
    <Compile Include="LargeClassWithTaskMethods_Exception.cs" />
  </ItemGroup>
</Project>
