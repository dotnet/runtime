<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <CLRTestTargetUnsupported Condition="'$(TargetArchitecture)' != 'wasm'">true</CLRTestTargetUnsupported>
    <EnableRuntimeAsyncOnWasm>true</EnableRuntimeAsyncOnWasm>
  </PropertyGroup>
  <ItemGroup>
    <MergedWrapperProjectReference Include="diagnostic-ip-stacktrace/diagnostic-ip-stacktrace.csproj" />
  </ItemGroup>

  <Import Project="$(TestSourceDir)MergedTestRunner.targets" />
</Project>
