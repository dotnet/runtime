<Project>

  <Import Project="..\Directory.Build.props" />

  <!-- This is only imported by helper tools which don't ship. -->
  <PropertyGroup>
    <IsShipping>false</IsShipping>
  </PropertyGroup>

</Project>
