<Project>
  <!-- OptData projects do not use the build targets of the .nuget directory. Instead they use the root build props -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.targets', '$(MSBuildThisFileDirectory)../../'))" />
</Project>