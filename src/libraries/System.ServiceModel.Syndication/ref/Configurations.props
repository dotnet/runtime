<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      net461;
      netcoreapp2.1;
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(NetCoreAppCurrent);
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>