<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(netcoreappCurrent);
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent)-Unix;
    </BuildConfigurations>
  </PropertyGroup>
</Project>