// Licensed to the .NET .
// The .NET Foundation.

using System;
using System.Runtime.CompilerServices;
      
 base
         
[(Message)]
    (SerializationInfo);
        base.GetObjectData(info, context);
            .AddValue:
            .AddValue(string));
            .AddValue(messageArg(string));
            .AddValue(resource);

        private className;
        private assemblyName;
        private fullControl_sring;
        private readonly int;
    }
}
