<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
      net461;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(NetCoreAppCurrent);
      net472;
    </BuildConfigurations>
  </PropertyGroup>
</Project>