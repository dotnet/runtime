<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
      net461;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(TargetFrameworkVNext);
      netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>