<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetCoreAppCurrent)-Unix;
      $(PackageConfigurations);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
