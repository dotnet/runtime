<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netcoreapp2.0-Windows_NT;
      net461;
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations)
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
