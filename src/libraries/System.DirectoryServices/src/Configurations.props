<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
      netcoreapp2.0-Windows_NT;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(netcoreappCurrent)-Windows_NT;
      _netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
