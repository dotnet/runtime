<Project>
  <PropertyGroup>
    <BuildConfigurations>
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent)-OSX;
      $(netcoreappCurrent)-Unix;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
