<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netstandard1.1;
      netstandard2.0;
      netcoreapp3.0-Windows_NT;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(TargetFrameworkVNext)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>