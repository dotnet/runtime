<Project>
  <PropertyGroup>
    <PackageConfigurations>
      net461;
      netstandard2.0;
      netstandard1.3;
      netstandard1.5;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      netfx;
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent)-Unix;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
