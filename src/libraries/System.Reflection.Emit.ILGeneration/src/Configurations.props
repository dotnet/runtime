<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
      netstandard1.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent)-Unix;
      $(PackageConfigurations);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
