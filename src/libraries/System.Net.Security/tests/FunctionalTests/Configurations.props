<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent)-Unix;
      $(netcoreappCurrent)-OSX;
    </BuildConfigurations>
  </PropertyGroup>
</Project>