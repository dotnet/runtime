<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(TargetFrameworkVNext)-Windows_NT;
      $(TargetFrameworkVNext)-Unix;
      $(TargetFrameworkVNext)-OSX;
    </BuildConfigurations>
  </PropertyGroup>
</Project>