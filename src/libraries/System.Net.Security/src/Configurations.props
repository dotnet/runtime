<Project>
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-OSX;
      $(NetCoreAppCurrent)-Unix;
      $(NetCoreAppCurrent)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
