<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(netcoreappCurrent)-Unix;
      $(netcoreappCurrent)-Windows_NT;
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>