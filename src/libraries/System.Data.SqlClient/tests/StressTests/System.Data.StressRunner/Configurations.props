<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Unix;
      $(NetCoreAppCurrent)-Windows_NT;
      net472-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>