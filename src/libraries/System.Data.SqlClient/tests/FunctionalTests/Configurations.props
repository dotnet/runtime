<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      net472-Windows_NT;
      $(NetCoreAppCurrent)-Unix;
      $(NetCoreAppCurrent)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>