<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      netfx-Windows_NT;
      $(TargetFrameworkVNext)-Unix;
      $(TargetFrameworkVNext)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>