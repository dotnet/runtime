<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      netfx-Windows_NT;
      $(netcoreappCurrent)-Unix;
      $(netcoreappCurrent)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>