<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      netstandard2.0;
      $(NetCoreAppCurrent);
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>