<Project>
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-FreeBSD;
      $(NetCoreAppCurrent)-Linux;
      $(NetCoreAppCurrent)-OSX;
      $(NetCoreAppCurrent)-Windows_NT;
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
