<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netcoreapp3.0-Unix;
      netcoreapp3.0;
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(netcoreappCurrent)-Unix;
      $(netcoreappCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>