<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netcoreapp3.0;
      netstandard2.1;
      net461;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations)
      $(NetFrameworkCurrent);
      $(NetCoreAppCurrent)      
    </BuildConfigurations>
  </PropertyGroup>
</Project>
