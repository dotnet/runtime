<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetCoreAppCurrent);
      net472-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
