<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(TargetFrameworkVNext)-Windows_NT;
      $(TargetFrameworkVNext);
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
