<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetCoreAppCurrent);
      $(NetFrameworkCurrent)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
