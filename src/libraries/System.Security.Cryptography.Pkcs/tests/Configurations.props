<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(netcoreappCurrent)-Windows_NT;
      $(netcoreappCurrent);
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
