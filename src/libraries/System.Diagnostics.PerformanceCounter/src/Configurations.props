<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netcoreapp2.0-Windows_NT;
      netstandard2.0;
      net461;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations)
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
