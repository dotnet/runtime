<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      netcoreapp5.0
      <!-- Use inbox facade on netfx -->
      _netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>