<Project>
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Linux;
      $(NetCoreAppCurrent)-OSX;
      $(NetCoreAppCurrent)-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>