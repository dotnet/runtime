<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      $(NetCoreAppCurrent)-Windows_NT;
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>