<Project>
  <PropertyGroup>
    <PackageConfigurations>
      net461;
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(NetFrameworkCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>