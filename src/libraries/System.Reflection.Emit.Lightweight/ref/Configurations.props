<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(netcoreappCurrent);
      <!-- Use inbox facade on netfx -->
      _netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>