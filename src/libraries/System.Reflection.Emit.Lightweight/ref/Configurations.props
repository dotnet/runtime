<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(TargetFrameworkVNext);
      <!-- Use inbox facade on netfx -->
      _netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>