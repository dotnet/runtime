<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <TargetFramework>$(NetCoreAppToolCurrent)</TargetFramework>
    <Nullable>enable</Nullable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <Import Project="..\..\..\subproject.props" />
  <Import Project="StressTests.targets" />

  <ItemGroup>
    <!-- Exclude debuggee source files — they are separate console apps -->
    <Compile Remove="Debuggees\**" />
    <!-- Exclude standalone analysis scripts — they use top-level statements -->
    <Compile Remove="analysis\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.DotNet.XUnitExtensions" Version="$(MicrosoftDotNetXUnitExtensionsVersion)" />
  </ItemGroup>
</Project>
