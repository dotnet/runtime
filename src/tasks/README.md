# MSBuild repository local tasks
This directory contains custom MSBuild tasks that are only relevant to dotnet/runtime and which don't contribute to restore inputs. Customized reusable tasks used during the build live in https://github.com/dotnet/arcade.
