const MonoBundledDataResource %Symbol% = {
    .resource = { .type = MONO_BUNDLED_DATA },
    .data = { .name = "%RegisteredName%",
              .data = %Symbol%_data,
              .size = %Len% },
};