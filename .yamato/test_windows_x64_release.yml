{% metadata_file .yamato/Globals.metafile %}
---
name: Test Windows x64 Release

agent:
  type: Unity::VM
  image: platform-foundation/coreclr-windows:v0.0.1-1276296
  flavor: b1.xlarge

dependencies:
  - path: .yamato/build_windows_x64_release.yml

commands:
# build/run tests
  - .yamato/scripts/build_yamato.cmd --arch=x64 --config=Release --test

