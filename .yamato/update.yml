# Auto-generated by Recipe Engine, do not modify manually.

update_from_upstream:
  name: Update from upstream
  agent:
    image: platform-foundation/linux-ubuntu-18.04-mono-bokken:0.1.7-978398
    type: Unity::VM
    flavor: b1.xlarge
  commands:
  - command: curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
  - command: ' echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null'
  - command: sudo apt update
  - command: sudo apt install gh
  - command: git config user.email "dotnet-vm-team-devs@unity3d.com"
  - command: git config user.name "Unity coreclr Bot"
  - command: .yamato/scripts/update_from_upstream.sh
  triggers:
    recurring:
    - branch: unity-main
      frequency: weekly

